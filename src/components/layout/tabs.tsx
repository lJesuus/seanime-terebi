//
// The phone-portrait BottomTabBar component was removed when we pruned the
// app down to Apple TV + Android TV. Navigation is driven by `SidebarShell`
// in `app/(app)/(tabs)/_layout.tsx`. This file is kept as a stub so a stray
// import path does not break the build before all callers are swept.
//
export const __legacyTabsRemoved = true as const
